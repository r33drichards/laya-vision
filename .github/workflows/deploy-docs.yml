name: Deploy Docs

# Builds the documentation site (site-docs/, with the browser demo from web-demo/ under demo/) with Nix and
# deploys it to GitHub Pages. The demo's model files are not part of the site: they load from the Hub
# (thaitea/laya-vision-web, published by `modal run modal_app.py::publish_web`).

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: DeterminateSystems/nix-installer-action@main
      - name: Build docs artifact
        run: nix build .#packages.x86_64-linux.docs --print-build-logs -L
      - name: Stage Pages artifact
        run: |
          mkdir public-docs
          cp -R result/. public-docs/
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: public-docs

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
