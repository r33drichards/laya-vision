{
  "name": "laya-vision-web-demo",
  "private": true,
  "type": "module",
  "description": "Test-only dependencies for the static demo; the page itself loads pinned CDN copies and needs no install.",
  "devDependencies": {
    "@huggingface/tokenizers": "0.2.0",
    "onnxruntime-web": "1.30.0",
    "playwright-core": "1.63.0"
  }
}
